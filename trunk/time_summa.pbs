#PBS -q class
#PBS -l nodes=8:sixcore
#PBS -l walltime=00:05:00
#PBS -N Proj1

export OMPI_MCA_mpi_yield_when_idle=0
cd $PBS_O_WORKDIR

date
hostname
echo $PWD
cat $PBS_NODEFILE
echo -e "\n\n"

export OMP_NUM_THREADS=1
export OMP_SCHEDULE="dynamic,1"

mpirun --hostfile $PBS_NODEFILE -np 64 ./time_summa -m 256 -n 256 -k 256 -x 8 -y 8 -b 1 -t 25 --openmp

# eof
